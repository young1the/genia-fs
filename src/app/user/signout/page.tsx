import SignOutBox from "@/components/user/SignOutBox/SignOutBox";

const page = () => {
  return (
    <section className='cc-page-wrapper'>
      <SignOutBox />
    </section>
  );
};

export default page;
