import RegisterForm from "@/components/user/RegisterForm/RegisterForm";

const page = () => {
  return (
    <section className='cc-page-wrapper'>
      <RegisterForm />
    </section>
  );
};

export default page;
