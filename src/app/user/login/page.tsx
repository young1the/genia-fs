import LoginForm from "@/components/user/LoginForm/LoginForm";

const page = () => {
  return (
    <section className='cc-page-wrapper'>
      <LoginForm />
    </section>
  );
};

export default page;
